id: platform.fontsource
label: Fontsource
slug: fontsource
short: "Fontsource는 Google Fonts나 기타 웹폰트를 자체 호스팅 방식으로 사용할 수 있게 해주는 npm 패키지 모음입니다. CDN 의존성 없이 프로젝트 번들에 폰트 파일을 직접 포함시켜 성능과 프라이버시를 개선합니다."
long: |-
  Fontsource는 전통적으로 Google Fonts CDN에서 폰트 CSS와 파일을 가져오는 방식의 대안으로 등장했습니다. CDN을 사용하면 간편하지만, 사용자의 위치에 따라 네트워크 지연이 발생하거나 특정 지역에서 CDN이 차단되거나 GDPR 등 프라이버시 규정에 민감한 경우 문제가 될 수 있습니다. Fontsource는 `npm install @fontsource/inter`처럼 패키지를 설치하면 `node_modules` 안에 폰트 파일과 CSS가 함께 들어오므로, 빌드 도구가 이 파일들을 최종 번들에 정적 자산으로 포함시킵니다. 이 방식은 CDN 의존성을 제거하고 배포 환경에서 폰트 사용을 예측 가능하게 만듭니다.

  설치 후에는 `import "@fontsource/inter"` 또는 특정 가중치만 불러오면 됩니다. Fontsource는 `font-display: swap`이나 `fallback` 같은 로딩 전략을 지원하고, subset처럼 특정 문자 집합만 가져오는 방식도 제공합니다. Google Fonts에 없는 폰트들도 커뮤니티 패키지로 제공되는 경우가 많습니다. 다만 모든 폰트 파일을 직접 호스팅하므로 CDN의 캐싱 이점을 잃을 수 있고, 빌드 크기가 커질 수 있어 전략적으로 선택해야 합니다.
example: null
translation_status: reviewed
aliases:
  - Fontsource
  - fontsource
  - "@fontsource"
  - 자체 호스팅 폰트
match_phrases:
  - phrase: Fontsource
    auto_match: true
    priority: 100
  - phrase: fontsource
    auto_match: true
    priority: 95
  - phrase: "@fontsource"
    auto_match: true
    priority: 90
  - phrase: 자체 호스팅 폰트
    auto_match: true
    priority: 80
