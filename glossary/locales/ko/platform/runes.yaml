id: platform.runes
label: Runes
slug: runes
short: "룬(Runes)은 Svelte 5에서 도입된 새로운 반응성 시스템으로, `$state`, `$derived`, `$effect` 등의 함수 형태로 값을 선언하고 의존성을 명시적으로 추적합니다. 기존의 `$:` 레이블 문법보다 명시적이고 TypeScript 친화적인 코드를 작성할 수 있습니다."
long: |-

  기존 Svelte는 컴파일러가 `let` 변수의 재할당을 감지하여 반응성을 자동으로 처리했지만, 복잡한 중첩 객체나 배열의 변경을 감지하는 데 한계가 있었습니다. 룬은 이러한 제약을 줄이기 위해 설계되었으며, `$state(initial)`로 상태를 선언하면 값이 변경되는 여러 지점에서 반응성이 트리거됩니다. `$derived`는 다른 상태 값을 기반으로 계산된 값을 자동으로 추적하며, `$effect`는 상태가 변경될 때마다 부수 효과를 실행합니다. 이러한 명시적인 선언은 복잡한 상태 흐름을 더 일관되게 표현할 수 있게 합니다.

  룬은 `.svelte` 파일뿐만 아니라 일반 `.js` 또는 `.ts` 파일에서도 동작하므로, 상태 로직을 컴포넌트 외부로 쉽게 추출하여 공유할 수 있습니다. 또한 기존의 `$:` 레이블 문법보다 명시적이고 TypeScript 친화적입니다. 복잡한 상태 흐름을 더 일관되게 표현할 수 있습니다. Svelte 5로 마이그레이션할 때는 기존 문법과 점진적으로 함께 사용할 수 있도록 호환성 레이어도 제공됩니다.
example: null
translation_status: reviewed
aliases:
  - Runes
  - runes
  - 룬
  - Svelte Runes
  - Svelte 5 Runes
match_phrases:
  - phrase: Runes
    auto_match: true
    priority: 100
  - phrase: Svelte Runes
    auto_match: true
    priority: 90
  - phrase: 룬
    auto_match: true
    priority: 80
