id: design.contrast-ratio
label: Contrast Ratio
slug: contrast-ratio
short: "Contrast Ratio(명도 대비비)는 텍스트와 배경 간의 밝기 차이를 수치로 나타낸 것입니다. 값이 높을수록 가독성이 좋으며, WCAG에서는 일반 텍스트에 최소 `4.5:1`을 요구합니다."
long: |-
  Contrast Ratio는 `(가장 밝은 색상의 휘도 + 0.05) / (가장 어두운 색상의 휘도 + 0.05)` 공식으로 계산됩니다. 휘도(luminance)는 sRGB 색상 공간에서 각 채널(R, G, B)을 감마 보정한 후 가중치(`0.2126 * R`, `0.7152 * G`, `0.0722 * B`)를 적용하여 구합니다. 계산 결과는 `1:1`(완전히 동일한 색상)에서 `21:1`(순수 검정 텍스트에 순수 흰색 배경 또는 그 반대) 사이의 값을 가집니다. `WCAG 2.1 SC 1.4.3`에 따르면 일반 텍스트(18px 미만 또는 14px 굵게 미만)는 `4.5:1` 이상, 대형 텍스트(18px 이상 또는 14px 굵게 이상)는 `3:1` 이상이어야 Level AA를 충족합니다.

  Contrast Ratio는 단순히 텍스트뿐만 아니라 아이콘, 그래픽, UI 컴포넌트의 경계와 포커스 링, 입력 필드의 플레이스홀더 등에도 적용됩니다. `SC 1.4.11` 비텍스트 대비는 Level AA에서 `3:1`을 요구합니다. 디자인 시스템에서 관리할 때는 텍스트와 배경의 실제 조합을 기준으로 판단해야 합니다. 색상 선택 시 `OKLCH`나 `Oklab` 색 공간을 사용하면 동일한 밝기 값을 유지한 채 색상만 변경해도 대비를 예측 가능하게 관리할 수 있습니다.
example: null
translation_status: reviewed
aliases:
  - Contrast Ratio
  - contrast ratio
  - 명도 대비비
  - 대비비
match_phrases:
  - phrase: Contrast Ratio
    auto_match: true
    priority: 100
  - phrase: 명도 대비비
    auto_match: true
    priority: 90
  - phrase: 대비비
    auto_match: true
    priority: 80
